---
name: list-apis
description: List Ax402 wrapped APIs for the current API key
---

Use MCP tool `ax402_list_apis`. Summarize name, slug, id, and upstream for each API. If the tool errors on missing `AX402_API_KEY`, follow the setup skill.
