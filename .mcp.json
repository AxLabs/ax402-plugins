{
  "mcpServers": {
    "ax402": {
      "command": "npx",
      "args": ["-y", "@ax402/mcp-server"],
      "env": {
        "AX402_API_KEY": "${AX402_API_KEY}",
        "AX402_BASE_URL": "${AX402_BASE_URL:-https://api.ax402.io}"
      }
    }
  }
}
